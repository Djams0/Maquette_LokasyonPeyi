import {icon,escape,showFlash} from '/ui/components.js';
const nav=[['/','home','Accueil'],['/rentals','calendar','Locations'],['/owner','car','Voiture'],['/messages','message','Messages'],['/profile','user','Profil']];
export function shell(me){const path=location.pathname;const active=href=>href==='/'?path==='/':path.startsWith(href);const tunnel=/^\/(inspection|request|payment)\//.test(path);document.getElementById('shell').innerHTML=`<div class="demo-banner">Maquette interactive · données fictives · aucun paiement réel <a href="/demo">Démo & comptes</a></div><header class="header"><a class="logo" href="/">Lokasyon <span>Péyi</span></a><nav aria-label="Navigation principale" id="header-nav">${me?nav.slice(0,4).map(([href,i,t])=>`<a class="${active(href)?'active':''}" href="${href}">${t==='Voiture'?'Mon espace loueur':t}</a>`).join(''):'<a href="/search">Nos véhicules</a>'}<a href="/faq">FAQ</a></nav><div class="tools">${me?`<a class="link-button button" href="/notifications" aria-label="Notifications">${icon('bell')}</a><a class="avatar" href="/profile" aria-label="Mon profil">${escape(me.name[0])}</a>`:`<a class="button warm" href="/owner/start">Louer ma voiture</a><a class="button secondary" href="/login">Connexion</a>`}<button class="mobile-menu" id="menu-toggle" aria-expanded="false" aria-controls="header-nav" aria-label="Ouvrir le menu">${icon('menu')}</button></div></header><main class="client-main" id="main" tabindex="-1"></main><footer class="footer"><div><a class="logo" href="/">Lokasyon <span>Péyi</span></a><p>Un autre chemin pour découvrir le Péyi.</p><small>Prototype indépendant · Guadeloupe</small></div><div class="row"><a href="/faq">FAQ</a><a href="/assistance">Nous contacter</a><a href="/design-system">Design system</a><a href="/demo">Guide de démonstration</a></div></footer>${!tunnel?`<nav class="bottom-nav" aria-label="Navigation mobile">${nav.map(([href,i,t])=>`<a href="${href}" class="${active(href)?'active':''}" ${active(href)?'aria-current="page"':''}>${icon(i)}${t}</a>`).join('')}</nav>`:''}`;document.getElementById('menu-toggle').onclick=e=>{const open=document.getElementById('header-nav').classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',open);};showFlash();}
export function ownerNav(){return `<nav class="subnav" aria-label="Espace propriétaire">${[['/owner','Vue d’ensemble'],['/owner/vehicles','Véhicules'],['/owner/requests','Demandes'],['/owner/revenue','Revenus'],['/owner/reputation','Réputation']].map(([h,t])=>`<a class="${location.pathname===h?'active':''}" href="${h}">${t}</a>`).join('')}</nav>`;}
